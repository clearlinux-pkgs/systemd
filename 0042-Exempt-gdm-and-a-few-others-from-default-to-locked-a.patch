From 8e978cc88af5d359362be2163de284b74aa26c79 Mon Sep 17 00:00:00 2001
From: Auke Kok <auke-jan.h.kok@intel.com>
Date: Thu, 10 Oct 2019 14:40:49 -0700
Subject: [PATCH 42/43] Exempt gdm and a few others from default to locked
 account.

---
 src/sysusers/sysusers.c | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/src/sysusers/sysusers.c b/src/sysusers/sysusers.c
index 08a2df707b..52886f5a02 100644
--- a/src/sysusers/sysusers.c
+++ b/src/sysusers/sysusers.c
@@ -532,6 +532,11 @@ static int write_temporary_shadow(const char *shadow_path, FILE **tmpfile, char
                         .sp_flag = (unsigned long) -1, /* this appears to be what everybody does ... */
                 };
 
+                /* exempt gdm, sddm, lightdm from locking by default */
+                /* https://github.com/systemd/systemd/issues/13522 */
+                if ((i->uid == 311) || (i->uid == 336) || (i->uid == 308))
+                        n.sp_expire = -1;
+
                 r = putspent_sane(&n, shadow);
                 if (r < 0)
                         return r;
-- 
2.24.0

