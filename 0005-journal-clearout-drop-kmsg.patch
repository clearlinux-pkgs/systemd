From cdb8ebaa7d1938b11e962647640f8003538f15d3 Mon Sep 17 00:00:00 2001
From: Dimitri John Ledkov <dimitri.j.ledkov@intel.com>
Date: Tue, 23 Jun 2015 11:25:41 +0100
Subject: [PATCH 05/36] journal: clearout & drop kmsg.

---
 src/journal/journald-kmsg.c | 9 +++++++++
 1 file changed, 9 insertions(+)

diff --git a/src/journal/journald-kmsg.c b/src/journal/journald-kmsg.c
index 5895e4870..339ecc611 100644
--- a/src/journal/journald-kmsg.c
+++ b/src/journal/journald-kmsg.c
@@ -393,6 +393,7 @@ static int dispatch_dev_kmsg(sd_event_source *es, int fd, uint32_t revents, void
 int server_open_dev_kmsg(Server *s) {
         mode_t mode;
         int r;
+        char buffer[40960];
 
         assert(s);
 
@@ -411,6 +412,14 @@ int server_open_dev_kmsg(Server *s) {
         if (!s->read_kmsg)
                 return 0;
 
+        /* clear out /dev/kmsg, we don't want all its messages */
+        while (1) {
+                int ret;
+                ret = read(s->dev_kmsg_fd, buffer, 40960);
+                if (ret <= 0)
+                        break;
+        }
+
         r = sd_event_add_io(s->event, &s->dev_kmsg_event_source, s->dev_kmsg_fd, EPOLLIN, dispatch_dev_kmsg, s);
         if (r < 0) {
 
-- 
2.17.0

